import faker from 'src/shared/utils/faker';
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';

import { {{pascalCase moduleName}} } from './entities/{{kebabCase moduleName}}.entity';
import { InternalServerException } from 'src/common/exceptions/http-exceptions';

@Injectable()
export class {{pascalCase moduleName}}Seed {
  constructor(
    @InjectRepository({{pascalCase moduleName}})
    private readonly {{camelCase moduleName}}Repository: Repository<{{pascalCase moduleName}}>,
  ) {}

  async seed{{pascalCase moduleName}}s(count: number): Promise<boolean> {
    try {
      const data = new Array(count).fill(null).map(() => {
        const {{camelCase moduleName}} = new {{pascalCase moduleName}}();
        // faker {{camelCase moduleName}} properties
        console.warn('Faker is not used!');
        console.log(faker.food.fruit());
        return {{camelCase moduleName}};
      });

      await this.{{camelCase moduleName}}Repository.save(data);

      return true;
    } catch (err: unknown) {
      console.log(err);
      throw new InternalServerException();
    }
  }
}
